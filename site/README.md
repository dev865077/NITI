# NITI Site

Static research site for NITI.

```sh
npm run site:build
npm run site:serve
```

The site is served from `site/index.html` and compiled TypeScript in
`site/dist/app.js`. The local server is compiled from `site/server.ts` into
`site/dist-server/server.js`.
